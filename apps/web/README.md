# SkillSet.ai Web

This directory contains the Next.js frontend for SkillSet.ai.

Important commands:

```bash
npm run dev
npm run lint
npm run typecheck
npm run build
npm run build:cloudflare
```

The frontend expects `SKILLSET_API_BASE_URL` to be available through Cloudflare runtime vars or `apps/web/.dev.vars`.
